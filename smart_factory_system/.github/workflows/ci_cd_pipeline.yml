name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov flake8

      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$(pwd)/smart_factory_system" >> $GITHUB_ENV  # Ajuste o PYTHONPATH

      - name: Run tests
        run: pytest --cov=. --cov-report=term-missing smart_factory_system/test_app.py  # Caminho correto para o arquivo de teste

      - name: Lint code with flake8
        run: flake8 .
